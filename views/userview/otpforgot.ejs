<!-- HEADER -->
<header>


  <%- include('header') -%>



    <!-- MAIN HEADER -->
    <div id="header">
      <!-- container -->
      <div class="container">
        <!-- row -->
        <div class="row   d-flex justify-content-center">
          <!-- LOGO -->
          <div class="col-md-3   d-flex justify-content-center">
            <div class="header-logo   d-flex justify-content-center">
              <a href="#" class="logo">
              </a>
            </div>
          </div>
          <!-- /LOGO -->


          <!-- /LOGin fo -->


          <div class="text-center text-dark center-div " id="login">



            <div class="row justify-content-center">
              <div class="col-12 col-md-6 col-lg-4" style="min-width: 500px">
                <div class="card bg-white mb-5 mt-5  border-border-primary" style="box-shadow: 0 12px 15px rgba(0, 0, 0, 0.02)">
                  <div class="card-body h5 text-center">
                    <h4>Two Step Authentication</h4>
                    <p>Your code was sent to you via email</p>
                    
                    <div class="container">
                      <div class="col-md-12 mx-auto">
                        <% if (typeof message !=='undefined' ) { %>
                          <div class="alert alert-success">
                            <%= message %>
                          </div>
                          <% } %>
                            <% if (typeof loginmessage !=='undefined' ) { %>
                              <div class="alert alert-danger">
                                <%= loginmessage %>
                              </div>
                              <% } %>
                      </div>
                    </div>



                    <form class="h3" action="/resetpassword" method="post">
                      <div class="otp-field mb-4">
                        <input type="number" placeholder="ENTER OTP"  inputmode="numeric" name="otpbox" id="otpform" />
                       
                      </div>

                      <div>
                        <p id="timer">Time remaining: <span id="countdown">0:00</span></p>
                        <a href="/resendotpforgot">        
                              <button  class="btn btn-warning" id="resendbutton" onclick="startTimer()">Resend OTP</button>
                        </a>
                      </div>

                      <button class="btn btn-primary p-3 rounded-pill mt-2 w-25" type="submit">Proceed</button>
                    </form>
                  </div>
                </div>
              </div>
            </div>




            
          </div>
        </div>
      </div>





      <header>







        <!-- FOOTER -->
      



          <!-- for otp timer -->

          <!-- <script>
            let countdownValue = 120; // Set the initial countdown value in seconds
          
            function startTimer() {
              const countdownElement = document.getElementById('countdown');
              const resendButton = document.getElementById('resendbutton');
          
              resendButton.disabled = true; // Disable the resend button during the countdown
          
              const countdownInterval = setInterval(() => {
                const minutes = Math.floor(countdownValue / 60);
                const seconds = countdownValue % 60;
          
                countdownElement.textContent = `${minutes}:${seconds < 10 ? '0' : ''}${seconds}`;
          
                if (countdownValue === 0) {
                  clearInterval(countdownInterval); // Stop the countdown
                  resendButton.disabled = false; // Enable the resend button
                } else {
                  countdownValue--;
                }
              }, 1000);
            }
          
            window.onload = startTimer; // Set window.onload to the startTimer function without invoking it
          </script>
           -->





           <script>
            let countdownValue = 120; // Set the initial countdown value in seconds
        
            function startTimer() {
                const countdownElement = document.getElementById('countdown');
                const resendButton = document.getElementById('resendbutton');
        
                resendButton.style.display = 'none'; // Display the resend button

                const countdownInterval = setInterval(() => {
                    const minutes = Math.floor(countdownValue / 60);
                    const seconds = countdownValue % 60;
        
                    countdownElement.textContent = `${minutes}:${seconds < 10 ? '0' : ''}${seconds}`;
        
                    if (countdownValue === 0) {
                        clearInterval(countdownInterval); // Stop the countdown
                        resendButton.style.display = 'inline-block'; 
                                          } else {
                        countdownValue--;
                    }
                }, 1000);
            }
        
            window.onload = startTimer; // Set window.onload to the startTimer function without invoking it
        </script>
        


        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">


        <style>
          .otp-field {
            flex-direction: row;
            column-gap: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
          }

          .otp-field input {
            height: 45px;
            width: 150px;
            border-radius: 6px;
            outline: none;
            font-size: 1.125rem;
            text-align: center;
            border: 1px solid #ddd;
          }

          .otp-field input:focus {
            box-shadow: 0 1px 0 rgba(0, 0, 0, 0.1);
          }

          .otp-field input::-webkit-inner-spin-button,
          .otp-field input::-webkit-outer-spin-button {
            display: none;
          }

          .resend {
            font-size: 12px;
          }

          .footer {
            position: absolute;
            bottom: 10px;
            right: 10px;
            color: black;
            font-size: 12px;
            text-align: right;
            font-family: monospace;
          }

          .footer a {
            color: black;
            text-decoration: none;
          }


          ::placeholder {
            font-size: 15px;
          }

           #otpform 
           {
            font-size: 20px;
           }
           #timer {
              font-size: 18px;
              font-weight: bold;
              color: #333;
            }

        </style>


        <%- include('footer') -%>